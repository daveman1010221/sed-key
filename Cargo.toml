[package]
name = "sed-key"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
linux-sed-opal-sys = "0.1.1"
libc = "0.2"
nix = { version = "0.27", default-features = false, features = ["ioctl"] }

[dev-dependencies]
proptest = "1.5"
